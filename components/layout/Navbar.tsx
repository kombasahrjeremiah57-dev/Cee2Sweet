'use client';
import Link from 'next/link'; import { useState } from 'react'; import { useCart } from '@/components/cart/CartProvider';
const links=[['Home','/'],['Shop','/shop'],['Categories','/categories/human-hair'],['New Arrivals','/shop?new=true'],['About','/about'],['Contact','/contact']];
export function Navbar(){const [open,setOpen]=useState(false);const {count}=useCart();return <><div className="bg-ink px-4 py-2 text-center text-[10px] font-semibold uppercase tracking-[.18em] text-white sm:text-xs">Premium Hair <span className="px-2 text-clay">•</span> Quality You Can Trust <span className="px-2 text-clay">•</span> Sierra Leone</div><header className="sticky top-0 z-40 border-b border-black/10 bg-cream/95 backdrop-blur"><div className="shell flex h-[72px] items-center justify-between"><Link href="/" className="font-display text-2xl font-bold tracking-tight">Cee2Sweet</Link><nav className="hidden gap-6 text-sm md:flex">{links.map(([n,h])=><Link key={n} href={h} className="transition hover:text-clay">{n}</Link>)}</nav><div className="flex items-center gap-4 text-sm"><Link href="/shop" aria-label="Search products" className="hidden transition hover:text-clay sm:block">Search</Link><span className="hidden text-stone-400 sm:block">Account</span><Link href="/cart" aria-label="Shopping cart" className="relative font-medium">Bag{count>0&&<b className="ml-1.5 inline-grid h-5 min-w-5 place-items-center rounded-full bg-ink px-1 text-[10px] text-white">{count}</b>}</Link><button className="text-xl md:hidden" onClick={()=>setOpen(!open)} aria-label="Toggle menu" aria-expanded={open}>{open?'×':'☰'}</button></div></div>{open&&<nav className="border-t border-black/10 bg-cream"><div className="shell flex flex-col gap-5 py-6 text-sm">{links.map(([n,h])=><Link onClick={()=>setOpen(false)} key={n} href={h}>{n}</Link>)}</div></nav>}</header></>}
