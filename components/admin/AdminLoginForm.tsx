'use client';
import { useState } from 'react';
import { useRouter, useSearchParams } from 'next/navigation';
import { createClient } from '@/lib/supabase/client';

export function AdminLoginForm(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const router=useRouter();const params=useSearchParams();const submit=async(event:React.FormEvent)=>{event.preventDefault();setError('');if(!process.env.NEXT_PUBLIC_SUPABASE_URL){setError('Supabase must be configured before admin sign-in can be enabled.');return}const result=await createClient().auth.signInWithPassword({email,password});if(result.error){setError('Unable to sign in with those details.');return}router.push('/admin');router.refresh()};return <div className="shell section"><form onSubmit={submit} className="mx-auto max-w-md rounded-2xl bg-white p-7 shadow-sm"><p className="eyebrow">Protected area</p><h1 className="mt-2 font-display text-4xl">Admin sign in</h1>{params.get('reason')==='setup'&&<p className="mt-4 rounded-lg bg-[#f7ece6] p-3 text-sm text-stone-700">Add Supabase environment variables to enable secure admin access.</p>}<label className="mt-6 block text-sm font-medium">Email<input required value={email} onChange={event=>setEmail(event.target.value)} type="email" className="mt-2 w-full rounded-xl border border-stone-300 p-3" /></label><label className="mt-4 block text-sm font-medium">Password<input required value={password} onChange={event=>setPassword(event.target.value)} type="password" className="mt-2 w-full rounded-xl border border-stone-300 p-3" /></label>{error&&<p role="alert" className="mt-4 text-sm text-red-700">{error}</p>}<button className="button button-dark mt-6 w-full">Sign in</button></form></div>}
